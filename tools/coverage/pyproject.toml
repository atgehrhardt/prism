## @file
## @brief Locked coverage tooling without optional source-checkout dependencies.
[project]
name = "prism-coverage"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["gcovr==8.6"]

[tool.uv]
package = false
