[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prism"
version = "0.0.0"
description = "Python tooling for Prism"
requires-python = ">=3.14"
license = {text = "GPL-3.0-only"}
authors = [
    {name = "LizardByte", email = "lizardbyte@users.noreply.github.com"}
]

dependencies = [
    "lizardbyte-common[c]",
]

[dependency-groups]
# glad2 declares Jinja2>=2.7,<4.0 as a dependency, so installing it pulls in jinja2 transitively.
# The platform marker limits this to Linux where the glad generator is actually used.
glad = [
    "glad2",
]

flatpak = [
    "flatpak_node_generator",
    "flatpak_pip_generator",
]

[project.urls]
Homepage = "https://github.com/atgehrhardt/prism"
Repository = "https://github.com/atgehrhardt/prism"
Issues = "https://github.com/atgehrhardt/prism/issues"

[tool.setuptools]
py-modules = []

[tool.uv.sources]
flatpak_node_generator = { path = "packaging/linux/flatpak/deps/flatpak-builder-tools/node" }
flatpak_pip_generator = { path = "packaging/linux/flatpak/deps/flatpak-builder-tools/pip" }
glad2 = { path = "third-party/glad" }
lizardbyte-common = { path = "third-party/lizardbyte-common" }
